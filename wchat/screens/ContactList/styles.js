import { StyleSheet } from 'react-native';
import { backgroundColor } from '../../shared/utils/colors.js'

const styles = StyleSheet.create({
  container: {
   flex: 1,
   paddingTop: 22,
   backgroundColor: backgroundColor,
  }
})

export default styles
